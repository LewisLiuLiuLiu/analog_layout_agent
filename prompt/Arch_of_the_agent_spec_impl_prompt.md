你现在的角色是：资深软件架构师 + 模拟 IC 版图工程师，负责把一个高层设计提案转写为可落地的工程实现规格。

【输入上下文】
- 我会提供一份完整的设计方案文档：《opamp_layout_gen_Agent_proposal.md》（下称“proposal”）。
- proposal 已经详细描述了三个核心问题：
  - 核心问题一：版图技巧知识的组织与应用（RAG + Skill 混合模式）
  - 核心问题二：Agent 的模块化架构设计（感知-规划-行动-记忆）
  - 核心问题三：自我迭代与进化机制
- 你的任务是基于 proposal 的内容，**生成“工程实现文档”的第 1 章：项目概述与架构设计**。

【输出目标】
输出一章工程化的“系统架构规格”，而不是重复解释 proposal。  
这章文档要回答的问题是：“如果要按这个方案真正落地系统，整体架构长什么样，各模块之间怎么交互？”

【输出内容结构要求（可用 Markdown 二级/三级标题组织）】
请至少包含以下小节，并可以在此基础上细化：
1. 项目概述
   - **项目目标**：用工程语言归纳本系统要实现的能力（围绕单级差分运放 MVP 场景）。
   - **MVP 范围**：明确说明当前版本仅支持的电路类型、工艺范围、DRC 规则范围。

2. 总体系统架构
   - 用文字 + 架构图（可以使用 Mermaid 文本描述）给出整体组件：
     - LLM / Planner
     - 感知模块（输入解析）
     - 行动模块（Skill / MCP 工具）
     - RAG 知识库
     - 记忆模块（短期/长期）
     - 评估模块（LLM-as-Judge）
   - 说明各组件部署形态（例如：本地进程 / 服务、是否需要独立进程）。

3. 模块划分与职责
   - 列出每个核心模块（Perception / Planning / Action / Memory / Evaluation / RAG / Skill Registry 等）的：
     - **职责描述**
     - **主要对外接口**（只写到接口名/功能，不需要 code）
     - **关键依赖**（例如依赖向量库、KLayout MCP、LLM API）

4. 模块接口与数据流向
   - 用“时序图级”的文字说明一个典型的 MVP 流程：
     - 读取网表 JSON + DRC JSON + 设计目标 → 解析成内部结构
     - 规划模块生成 TODO/Plan
     - 行动模块调用 Skill / KLayout MCP
     - RAG 检索参与规划
     - 评估模块与 Memory 的调用
   - 明确每一步之间的数据对象类型（例如：`Circuit` 对象、`PlanStep[]`、`ToolCallRequest`、`ToolCallResult` 等）。

5. 与后续章节的关系说明
   - 简短说明：后续章节（RAG、Skill、Agent 核心逻辑、输入输出处理、测试、部署）都是围绕本架构展开的子规格。
   - 标明一些要在后文细化的名词/接口（例如 `Circuit`、`DRCRuleSet`、`SkillRegistry`），保证命名在整份实现文档中保持一致。

【技术深度要求】
- 不要求给出具体代码，但要写到“**组件级 + 接口级**”粒度：
  - 组件之间如何调用，用什么数据结构大致传递。
  - 可以给出伪接口签名（例如：`plan(circuit: Circuit, objectives: Objectives) -> Plan`）。
- 允许你在不与 proposal 冲突的前提下，对缺失的工程细节做理性补充，但必须在文档中显式标明是“推荐实现方式”或“工程假设”。

【与整体项目的关联性要求】
- 所有模块命名、职责划分必须与 proposal 中的描述保持语义一致。
- 架构必须围绕 proposal 中的 MVP 场景（单级差分运放自动版图生成）展开，不要提前扩展到多级运放或更复杂系统。